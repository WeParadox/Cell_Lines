# Cell Line Repository (MkDocs Material)

Static website hosted on GitHub Pages with culture/handling notes for cell lines.

## Local preview
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
mkdocs serve
